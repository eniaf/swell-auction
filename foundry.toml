[profile.default]
src = "contracts"
test = "test"
out = "out"
libs = ["lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200
via_ir = true

[profile.default.fuzz]
runs = 256

# For fork tests - set your RPC in .env
# [profile.fork]
# fork_url = "${RPC_URL}"
